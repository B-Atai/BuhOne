$('.slider').slick({
    infinite: true,
    speed: 500,
    fade: true,
    cssEase: 'linear',
    dots:true
  });

$('.clients__items').slick({
    infinite: true,
    speed: 500,
    slidesToShow: 1,
    slidestoScroll:1,
    dots:true
});